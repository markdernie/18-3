import { RandomPlaceholderImagePipe } from './random-placeholder-image.pipe';

describe('RandomPlaceholderImagePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomPlaceholderImagePipe();
    expect(pipe).toBeTruthy();
  });
});
